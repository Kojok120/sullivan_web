name: Deploy PRODUCTION

on:
  push:
    branches: [main]

# 本番デプロイは同時実行を防ぐ
concurrency:
  group: deploy-production
  cancel-in-progress: false

env:
  NODE_VERSION: "20"
  GCP_REGION: asia-northeast1

jobs:
  deploy-web:
    name: Deploy Web App (PRODUCTION)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [migrate-db, deploy-grading-worker]
    environment: production
    permissions:
      contents: read
      id-token: write

    steps:
      - name: チェックアウト
        uses: actions/checkout@v4

      - name: GCP 認証 (Workload Identity Federation)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: gcloud CLI セットアップ
        uses: google-github-actions/setup-gcloud@v2

      - name: 必須シークレット検証 (Web)
        env:
          GEMINI_MODEL: ${{ secrets.GEMINI_MODEL }}
          GEMINI_CHAT_MODEL: ${{ secrets.GEMINI_CHAT_MODEL }}
          GRADING_WORKER_URL: ${{ secrets.GRADING_WORKER_URL }}
          INTERNAL_API_SECRET: ${{ secrets.INTERNAL_API_SECRET }}
          QSTASH_TOKEN: ${{ secrets.QSTASH_TOKEN }}
          QSTASH_CURRENT_SIGNING_KEY: ${{ secrets.QSTASH_CURRENT_SIGNING_KEY }}
          QSTASH_NEXT_SIGNING_KEY: ${{ secrets.QSTASH_NEXT_SIGNING_KEY }}
        run: |
          for var in GEMINI_MODEL GEMINI_CHAT_MODEL GRADING_WORKER_URL INTERNAL_API_SECRET QSTASH_TOKEN QSTASH_CURRENT_SIGNING_KEY QSTASH_NEXT_SIGNING_KEY; do
            if [ -z "${!var}" ]; then
              echo "Missing required secret: ${var}"
              exit 1
            fi
          done

      - name: Docker 認証
        run: gcloud auth configure-docker asia.gcr.io --quiet

      - name: Docker ビルド (Web)
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_SUPABASE_URL=${{ secrets.NEXT_PUBLIC_SUPABASE_URL }} \
            --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }} \
            --build-arg NEXT_PUBLIC_GEMINI_SILENCE_HOLD_MS=${{ secrets.NEXT_PUBLIC_GEMINI_SILENCE_HOLD_MS }} \
            --build-arg QSTASH_CURRENT_SIGNING_KEY=${{ secrets.QSTASH_CURRENT_SIGNING_KEY }} \
            --build-arg QSTASH_NEXT_SIGNING_KEY=${{ secrets.QSTASH_NEXT_SIGNING_KEY }} \
            -t asia.gcr.io/${{ secrets.GCP_PROJECT_ID }}/sullivan-app-production:${{ github.sha }} \
            .

      - name: Docker プッシュ (Web)
        run: docker push asia.gcr.io/${{ secrets.GCP_PROJECT_ID }}/sullivan-app-production:${{ github.sha }}

      - name: Cloud Run デプロイ (Web)
        run: |
          gcloud run deploy sullivan-app-production \
            --project "${{ secrets.GCP_PROJECT_ID }}" \
            --service-account "${{ secrets.RUNTIME_SA_EMAIL }}" \
            --image asia.gcr.io/${{ secrets.GCP_PROJECT_ID }}/sullivan-app-production:${{ github.sha }} \
            --platform managed \
            --region ${{ env.GCP_REGION }} \
            --memory 4Gi \
            --cpu 2 \
            --min 0 \
            --concurrency 20 \
            --timeout 120s \
            --allow-unauthenticated \
            --update-env-vars "BIND_HOST=0.0.0.0,SERVICE_ROLE=web" \
            --update-env-vars "NODE_ENV=production" \
            --update-secrets "DATABASE_URL=database-url:latest" \
            --update-secrets "DIRECT_URL=direct-url:latest" \
            --update-env-vars "QSTASH_TOKEN=${{ secrets.QSTASH_TOKEN }}" \
            --update-env-vars "QSTASH_CURRENT_SIGNING_KEY=${{ secrets.QSTASH_CURRENT_SIGNING_KEY }}" \
            --update-env-vars "QSTASH_NEXT_SIGNING_KEY=${{ secrets.QSTASH_NEXT_SIGNING_KEY }}" \
            --update-env-vars "UPSTASH_REDIS_REST_URL=${{ secrets.UPSTASH_REDIS_REST_URL }}" \
            --update-env-vars "UPSTASH_REDIS_REST_TOKEN=${{ secrets.UPSTASH_REDIS_REST_TOKEN }}" \
            --update-env-vars "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" \
            --update-env-vars "GEMINI_MODEL=${{ secrets.GEMINI_MODEL }}" \
            --update-env-vars "GEMINI_CHAT_MODEL=${{ secrets.GEMINI_CHAT_MODEL }}" \
            --update-env-vars "GEMINI_CHAT_FALLBACK_MODEL=${{ secrets.GEMINI_CHAT_FALLBACK_MODEL }}" \
            --update-env-vars "GEMINI_LIVE_MODEL=${{ secrets.GEMINI_LIVE_MODEL }}" \
            --update-env-vars "GEMINI_LIVE_API_VERSION=${{ secrets.GEMINI_LIVE_API_VERSION }}" \
            --update-env-vars "GEMINI_LIVE_VOICE=${{ secrets.GEMINI_LIVE_VOICE }}" \
            --update-env-vars "DRIVE_FOLDER_ID=${{ secrets.DRIVE_FOLDER_ID }}" \
            --update-env-vars "APP_URL=${{ secrets.APP_URL }}" \
            --update-env-vars "GOOGLE_CLOUD_PROJECT_ID=${{ secrets.GCP_PROJECT_ID }}" \
            --update-env-vars "CLOUD_RUN_REGION=${{ env.GCP_REGION }}" \
            --update-env-vars "NEXT_PUBLIC_SUPABASE_URL=${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}" \
            --update-env-vars "NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" \
            --update-env-vars "NEXT_PUBLIC_GEMINI_SILENCE_HOLD_MS=${{ secrets.NEXT_PUBLIC_GEMINI_SILENCE_HOLD_MS }}" \
            --update-env-vars "SUPABASE_SERVICE_ROLE_KEY=${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            --update-secrets "INTERNAL_API_SECRET=internal-api-secret:latest" \
            --update-secrets "DRIVE_WEBHOOK_TOKEN=drive-webhook-token:latest" \
            --update-env-vars "DRIVE_WEBHOOK_CHANNEL_ID=${{ secrets.DRIVE_WEBHOOK_CHANNEL_ID }}" \
            --update-env-vars "GRADING_WORKER_URL=${{ secrets.GRADING_WORKER_URL }}"

      - name: Runtime SA に Cloud Run 更新権限を付与
        run: |
          gcloud run services add-iam-policy-binding sullivan-app-production \
            --project "${{ secrets.GCP_PROJECT_ID }}" \
            --region ${{ env.GCP_REGION }} \
            --member "serviceAccount:${{ secrets.RUNTIME_SA_EMAIL }}" \
            --role "roles/run.admin" \
            --quiet

      - name: Cloud Scheduler 更新 (Web)
        env:
          APP_INTERNAL_SECRET: ${{ secrets.INTERNAL_API_SECRET }}
        run: |
          set -euo pipefail

          upsert_scheduler_job() {
            local job_name="$1"
            local schedule="$2"
            local uri="$3"
            local message_body="${4:-}"
            local header_arg="Authorization=Bearer ${APP_INTERNAL_SECRET},Content-Type=application/json"

            if gcloud scheduler jobs describe "$job_name" --location "${{ env.GCP_REGION }}" --project "${{ secrets.GCP_PROJECT_ID }}" >/dev/null 2>&1; then
              local update_args=(
                scheduler jobs update http "$job_name"
                --location "${{ env.GCP_REGION }}"
                --project "${{ secrets.GCP_PROJECT_ID }}"
                --schedule "$schedule"
                --time-zone "Asia/Tokyo"
                --uri "$uri"
                --http-method POST
                --update-headers "$header_arg"
              )

              if [ -n "$message_body" ]; then
                update_args+=(--message-body "$message_body")
              else
                update_args+=(--clear-message-body)
              fi

              gcloud "${update_args[@]}"
              return 0
            fi

            local create_args=(
              scheduler jobs create http "$job_name"
              --location "${{ env.GCP_REGION }}"
              --project "${{ secrets.GCP_PROJECT_ID }}"
              --schedule "$schedule"
              --time-zone "Asia/Tokyo"
              --uri "$uri"
              --http-method POST
              --headers "$header_arg"
            )

            if [ -n "$message_body" ]; then
              create_args+=(--message-body "$message_body")
            fi

            gcloud "${create_args[@]}"
          }

          SERVICE_URL="$(gcloud run services describe sullivan-app-production \
            --project "${{ secrets.GCP_PROJECT_ID }}" \
            --region "${{ env.GCP_REGION }}" \
            --format='value(status.url)')"

          if [ -z "$SERVICE_URL" ]; then
            echo "Failed to resolve Cloud Run service URL"
            exit 1
          fi

          upsert_scheduler_job \
            "sullivan-app-warm-start" \
            "0 15 * * 1-5" \
            "${SERVICE_URL}/api/internal/cloud-run/min-instances" \
            '{"minInstances":1,"reason":"weekday-warm-start"}'

          upsert_scheduler_job \
            "sullivan-app-warm-stop" \
            "0 22 * * 1-5" \
            "${SERVICE_URL}/api/internal/cloud-run/min-instances" \
            '{"minInstances":0,"reason":"weekday-warm-stop"}'

          upsert_scheduler_job \
            "sullivan-drive-watch-renew" \
            "30 15,21 * * 1-5" \
            "${SERVICE_URL}/api/drive/watch/renew?check=1"

  deploy-grading-worker:
    name: Deploy Grading Worker (PRODUCTION)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [migrate-db]
    environment: production
    permissions:
      contents: read
      id-token: write

    steps:
      - name: チェックアウト
        uses: actions/checkout@v4

      - name: GCP 認証 (Workload Identity Federation)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: gcloud CLI セットアップ
        uses: google-github-actions/setup-gcloud@v2

      - name: 必須シークレット検証 (Grading Worker)
        env:
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          RUNTIME_SA_EMAIL: ${{ secrets.RUNTIME_SA_EMAIL }}
          UPSTASH_REDIS_REST_URL: ${{ secrets.UPSTASH_REDIS_REST_URL }}
          UPSTASH_REDIS_REST_TOKEN: ${{ secrets.UPSTASH_REDIS_REST_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: ${{ secrets.GEMINI_MODEL }}
          GEMINI_CHAT_MODEL: ${{ secrets.GEMINI_CHAT_MODEL }}
          GEMINI_LIVE_MODEL: ${{ secrets.GEMINI_LIVE_MODEL }}
          GEMINI_LIVE_API_VERSION: ${{ secrets.GEMINI_LIVE_API_VERSION }}
          GEMINI_LIVE_VOICE: ${{ secrets.GEMINI_LIVE_VOICE }}
          DRIVE_FOLDER_ID: ${{ secrets.DRIVE_FOLDER_ID }}
          GRADING_WORKER_URL: ${{ secrets.GRADING_WORKER_URL }}
          QSTASH_TOKEN: ${{ secrets.QSTASH_TOKEN }}
          QSTASH_CURRENT_SIGNING_KEY: ${{ secrets.QSTASH_CURRENT_SIGNING_KEY }}
          QSTASH_NEXT_SIGNING_KEY: ${{ secrets.QSTASH_NEXT_SIGNING_KEY }}
        run: |
          for var in \
            GCP_PROJECT_ID \
            RUNTIME_SA_EMAIL \
            UPSTASH_REDIS_REST_URL \
            UPSTASH_REDIS_REST_TOKEN \
            GEMINI_API_KEY \
            GEMINI_MODEL \
            GEMINI_CHAT_MODEL \
            GEMINI_LIVE_MODEL \
            GEMINI_LIVE_API_VERSION \
            GEMINI_LIVE_VOICE \
            DRIVE_FOLDER_ID \
            GRADING_WORKER_URL \
            QSTASH_TOKEN \
            QSTASH_CURRENT_SIGNING_KEY \
            QSTASH_NEXT_SIGNING_KEY; do
            if [ -z "${!var}" ]; then
              echo "Missing required secret: ${var}"
              exit 1
            fi
          done

      - name: Docker 認証
        run: gcloud auth configure-docker asia.gcr.io --quiet

      - name: Docker ビルド (Grading Worker)
        run: |
          docker build \
            -f Dockerfile.worker \
            -t asia.gcr.io/${{ secrets.GCP_PROJECT_ID }}/sullivan-grading-worker-production:${{ github.sha }} \
            .

      - name: Docker プッシュ (Grading Worker)
        run: docker push asia.gcr.io/${{ secrets.GCP_PROJECT_ID }}/sullivan-grading-worker-production:${{ github.sha }}

      - name: Cloud Run デプロイ (Grading Worker)
        run: |
          gcloud run deploy sullivan-grading-worker-production \
            --project "${{ secrets.GCP_PROJECT_ID }}" \
            --service-account "${{ secrets.RUNTIME_SA_EMAIL }}" \
            --image asia.gcr.io/${{ secrets.GCP_PROJECT_ID }}/sullivan-grading-worker-production:${{ github.sha }} \
            --platform managed \
            --region ${{ env.GCP_REGION }} \
            --memory 4Gi \
            --cpu 2 \
            --concurrency 1 \
            --min-instances 10 \
            --max-instances 50 \
            --allow-unauthenticated \
            --set-env-vars "NODE_ENV=production,SERVICE_ROLE=worker" \
            --update-secrets "DATABASE_URL=database-url:latest" \
            --update-secrets "DIRECT_URL=direct-url:latest" \
            --update-secrets "INTERNAL_API_SECRET=internal-api-secret:latest" \
            --set-env-vars "UPSTASH_REDIS_REST_URL=${{ secrets.UPSTASH_REDIS_REST_URL }}" \
            --set-env-vars "UPSTASH_REDIS_REST_TOKEN=${{ secrets.UPSTASH_REDIS_REST_TOKEN }}" \
            --set-env-vars "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" \
            --set-env-vars "GEMINI_MODEL=${{ secrets.GEMINI_MODEL }}" \
            --set-env-vars "GEMINI_CHAT_MODEL=${{ secrets.GEMINI_CHAT_MODEL }}" \
            --set-env-vars "GEMINI_CHAT_FALLBACK_MODEL=${{ secrets.GEMINI_CHAT_FALLBACK_MODEL }}" \
            --set-env-vars "GEMINI_LIVE_MODEL=${{ secrets.GEMINI_LIVE_MODEL }}" \
            --set-env-vars "GEMINI_LIVE_API_VERSION=${{ secrets.GEMINI_LIVE_API_VERSION }}" \
            --set-env-vars "GEMINI_LIVE_VOICE=${{ secrets.GEMINI_LIVE_VOICE }}" \
            --set-env-vars "DRIVE_FOLDER_ID=${{ secrets.DRIVE_FOLDER_ID }}" \
            --set-env-vars "GRADING_WORKER_URL=${{ secrets.GRADING_WORKER_URL }}" \
            --set-env-vars "QSTASH_TOKEN=${{ secrets.QSTASH_TOKEN }}" \
            --set-env-vars "QSTASH_CURRENT_SIGNING_KEY=${{ secrets.QSTASH_CURRENT_SIGNING_KEY }}" \
            --set-env-vars "QSTASH_NEXT_SIGNING_KEY=${{ secrets.QSTASH_NEXT_SIGNING_KEY }}"

  migrate-db:
    name: DB マイグレーション (PRODUCTION)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      DIRECT_URL: ${{ secrets.DIRECT_URL }}

    steps:
      - name: チェックアウト
        uses: actions/checkout@v4

      - name: Node.js セットアップ
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: 依存関係インストール
        run: npm ci

      - name: Prisma マイグレーション実行
        run: npx prisma migrate deploy

  notify-discord:
    name: Discord 通知 (PRODUCTION)
    runs-on: ubuntu-latest
    needs: [migrate-db, deploy-grading-worker, deploy-web]
    if: always()
    environment: production

    steps:
      - name: Discord Webhook 設定確認
        id: webhook
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          if [ -z "$DISCORD_WEBHOOK_URL" ]; then
            echo "::warning::DISCORD_WEBHOOK_URL が未設定のため、Discord 通知をスキップします。"
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "enabled=true" >> "$GITHUB_OUTPUT"

      - name: Discord に通知
        if: steps.webhook.outputs.enabled == 'true'
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          DISCORD_ERROR_MENTION: ${{ secrets.DISCORD_ERROR_MENTION }}
          MIGRATE_RESULT: ${{ needs.migrate-db.result }}
          WORKER_RESULT: ${{ needs.deploy-grading-worker.result }}
          WEB_RESULT: ${{ needs.deploy-web.result }}
        run: |
          STATUS="成功"
          MENTION_PREFIX=""
          ALLOWED_MENTIONS='{"parse":[]}'
          if [ "$MIGRATE_RESULT" != "success" ] || [ "$WORKER_RESULT" != "success" ] || [ "$WEB_RESULT" != "success" ]; then
            STATUS="失敗"
            if [ -n "$DISCORD_ERROR_MENTION" ]; then
              MENTION_PREFIX="$(printf '%s\n' "$DISCORD_ERROR_MENTION")"
              ALLOWED_MENTIONS='{"parse":["users","roles"]}'
            fi
          fi

          SHORT_SHA="$(printf '%s' "$GITHUB_SHA" | cut -c1-7)"
          RUN_URL="https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          COMMIT_URL="https://github.com/${GITHUB_REPOSITORY}/commit/${GITHUB_SHA}"

          MESSAGE_BODY="$(printf '%s\n' \
            "[Sullivan] production デプロイ${STATUS}" \
            "repo: ${GITHUB_REPOSITORY}" \
            "branch: ${GITHUB_REF_NAME}" \
            "actor: ${GITHUB_ACTOR}" \
            "commit: ${SHORT_SHA} ${COMMIT_URL}" \
            "web: ${WEB_RESULT}" \
            "worker: ${WORKER_RESULT}" \
            "db: ${MIGRATE_RESULT}" \
            "run: ${RUN_URL}")"

          if [ -n "$MENTION_PREFIX" ]; then
            MESSAGE="$(printf '%s\n%s' "$MENTION_PREFIX" "$MESSAGE_BODY")"
          else
            MESSAGE="$MESSAGE_BODY"
          fi

          PAYLOAD="$(jq -nc \
            --arg content "$MESSAGE" \
            --argjson allowedMentions "$ALLOWED_MENTIONS" \
            '{content: $content, allowed_mentions: $allowedMentions}')"
          curl -fsS -H "Content-Type: application/json" -d "$PAYLOAD" "$DISCORD_WEBHOOK_URL"
